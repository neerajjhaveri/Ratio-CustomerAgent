/**
 * Custom React hooks.
 *
 * Place reusable hooks here as the app grows, e.g.:
 *   hooks/useApi.ts        — generic fetch-with-state hook
 *   hooks/usePolling.ts    — interval-based data refresh
 *   hooks/useAuth.ts       — authentication state
 *
 * Re-export from this barrel:
 *   import { useApi } from '../hooks';
 */

export {};
