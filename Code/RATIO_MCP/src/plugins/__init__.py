"""Plugins package for config-driven tool implementations.

Drop new plugin modules here and reference them in tools_config.json.
Each plugin module must expose an async entry function (default: 'run').
"""
